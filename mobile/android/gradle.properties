org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true
# The project lives under a path with CJK characters; allow the Android build to proceed.
android.overridePathCheck=true
# Kotlin's incremental compiler cannot handle CJK characters in its cache paths,
# so disable incremental compilation (and the build cache) for a clean APK build here.
kotlin.incremental=false
org.gradle.caching=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
